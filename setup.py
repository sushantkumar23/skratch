from setuptools import setup, find_packages

setup(name = 'skratch',version = '0.1',
      url = https://github.com/sushantkumar23/skratch,
      packages=find_packages(),
      install_requires=[
          'numpy','pandas','tensorflow'
      ],
      include_package_data=True,
      zip_safe=False)
     
